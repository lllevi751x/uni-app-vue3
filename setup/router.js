export const config = {  loginPage: '/main/pages/login/index'}/** * 页面跳转拦截 * @description 监听由路由切换API触发的页面跳转 */export const setupRoute = () => {  const list = ['navigateTo', 'redirectTo', 'reLaunch', 'switchTab', 'navigateBack']    list.forEach(item => {    uni.addInterceptor(item, {      invoke: e => {        console.log('>>>>', e)        return e      }    })  })}